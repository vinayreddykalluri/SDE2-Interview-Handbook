# Printable Book Templates

This directory owns inputs used only for PDF and DOCX presentation.

- `metadata.yaml` defines shared book metadata.
- `pdf-cover.html` defines the PDF cover fragment.
- `reference.docx` defines Word styles and document defaults.

Curriculum prose belongs in `docs/`. Generated books belong in ignored `output/` directories and must not be committed.

