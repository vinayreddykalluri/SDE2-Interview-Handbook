# Python Dependency Manifests

| Manifest | Consumer |
|---|---|
| `authoring.txt` | Local validation, MkDocs, PDF, and DOCX authoring |
| `portal.txt` | Minimal Vercel static-site build |

Both manifests use exact pins. Update the deployment validator and `vercel.json` if the portal manifest contract changes.
